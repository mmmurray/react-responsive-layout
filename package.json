{
  "name": "react-responsive-layout",
  "version": "0.1.0",
  "author": "Mark Murray <mark@murray.xyz>",
  "repository": "git@github.com:mmmurray/react-responsive-layout.git",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --rootDir src --outDir lib",
    "jest": "jest --watch",
    "prettier": "prettier --write '{src/**/*,test/**/*}.{ts,json}'",
    "test:compile": "tsc -p . --noEmit",
    "test:coverage": "jest --coverage",
    "test": "run-p test:*"
  },
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "jest": "^23.6.0",
    "npm-run-all": "^4.1.3",
    "prettier": "^1.15.2",
    "ts-jest": "^23.10.4",
    "typescript": "^3.1.6"
  }
}
